1. step to run mysqlDBbackup.sh
	- Create a "cred" file in local folder from where you want to run script.
		Ex -
			cat cred
			username=dbuser
			password=welcome1
2. Run the script with DB name as Argument to it.
	- sh.mysqlDBbackup.sh db_sanpdeal
